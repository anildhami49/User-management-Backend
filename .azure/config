# Azure App Service Configuration
# This file tells Azure how to configure the Python app

[build]
  command = "pip install -r requirements.txt"

[run]
  # Gunicorn command for running Flask app
  command = "gunicorn --bind=0.0.0.0:8000 --workers=4 --timeout=600 app:app"
